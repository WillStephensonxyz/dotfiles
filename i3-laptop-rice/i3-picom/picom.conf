backend = "glx";
glx-no-stencil = true;
glx-copy-from-front = false;

active-opacity = 1;
inactive-opacity = 1;
frame-opacity = 1;
inactive-opacity-overide = false;

opacity-rule = [
	"80:class_g = 'URxvt'"
];

# blur-method = "dual_kawase";
blue-strength = 6;
